
# Guide-zzyz

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.

## 仓库结构（截至至2026.7.27）

>这个仓库国内外ai都可访问，所以看仓库结构可以直接把网址扔给ai，让它分析。开发者需要尽快快速熟悉 `guide-zzyz-web`。在正式动手写代码之前，可以通过以下内容快速了解项目的代码结构和开发规范。


### 一、 项目根目录概览
项目的根目录下主要包含了一些配置文件，它们决定了项目如何运行、编译和打包：

#### 🟢 核心开发区【可修改】
这里是日常编写业务代码的地方，你可以放心在这里创建、修改和删除文件。

*   **`src/`** ➡️ **【可修改】**
    *   *说明*：最核心的源码目录。你所有的页面、组件、样式、API 接口和业务逻辑都在这里编写。
*   **`public/`** ➡️ **【可修改】**
    *   *说明*：存放全局静态资源的目录。如果需要更换网站的图标（`favicon.ico`）或者放入一些不希望被编译的特殊文件，可以放在这里。
*   **`README.md`** ➡️ **【可修改】**
    *   *说明*：项目的说明文档。如果你在开发中发现了新的注意事项，或者补充了新的运行步骤，可以随时修改并完善它。


#### ⚠️ 配置文件区【谨慎修改 / 需指导】
这些文件决定了整个项目的运行规则、依赖和类型系统。小白开发者在没有导师指导的情况下，**请勿自行修改**。

*   **`package.json`** ➡️ **【谨慎修改】**
    *   *说明*：项目的依赖和脚本配置文件。如果需要安装新的第三方库，请使用命令（如 `npm install xxx`）让系统自动写入，不要手动编辑该文件。
*   **`vite.config.ts`** ➡️ **【谨慎修改】**
    *   *说明*：Vite 构建工具的配置文件（如配置开发代理、打包优化等）。如果遇到本地开发跨域、需要配置 `proxy` 时，请在导师指导下修改。
*   **`index.html`** ➡️ **【谨慎修改】**
    *   *说明*：网页的唯一入口。通常只有在需要引入全局的外部 CDN 脚本、修改网页标题或添加全局全局埋点时才需要修改。
*   **`.gitignore`** ➡️ **【谨慎修改】**
    *   *说明*：规定哪些文件不需要提交到 Git（如 `node_modules`）。如果新增了敏感的配置文件或本地临时文件，需要在此处添加忽略规则。


#### 🔴 自动生成与敏感区【别动】
这些文件由系统、编译器或打包工具自动生成，手动修改它们会导致项目报错、运行失败或代码丢失。

*   **`node_modules/`** ➡️ **【别动】**
    *   *说明*：存放项目所有第三方依赖包的目录。由 `npm install` 自动生成，**千万不要手动修改里面的代码**，否则别人拉取代码后无法复现你的改动。
*   **`package-lock.json`** ➡️ **【别动】**
    *   *说明*：锁定依赖包具体版本的文件。由 npm 自动生成和更新，手动修改该文件极易导致团队成员的依赖版本冲突。
*   **`build/`** ➡️ **【别动】**
    *   *说明*：执行 `npm run build` 后自动生成的打包产物。手动修改里面的代码没有任何意义，因为下次重新打包时，这里的代码会被完全覆盖。
*   **`tsconfig.json` & `tsconfig.node.json`** ➡️ **【别动】**
    *   *说明*：TypeScript 编译器的配置文件。修改可能导致项目大面积报红、类型报错或无法正常打包。
 
---

### 二、 最主要的代码在哪里？`src/` 目录解密
对于前端开发者而言，日常 **95% 以上的工作都在 `src` 文件夹中进行**。在该类项目中，`src` 通常会按照功能角色划分为以下几个核心子文件夹：

#### 1. 核心入口
*   **`src/main.tsx`** (或 `index.tsx`)：整个应用的 JavaScript/TypeScript 入口，负责初始化框架并将应用挂载到 HTML 的 `div#root` 上。
*   **`src/App.tsx`**：应用的根组件。全局的路由配置、主题配置、状态管理提供者（Providers）通常会包裹在这里。

#### 2. 常见子文件夹及功能分工
*   **`src/pages/`** (或 `views/`)
    *   **职责**：存放各个独立的“业务页面”（如：首页、用户中心、指南详情页等）。
    *   **特点**：每个页面通常对应一个单独的文件夹。
*   **`src/components/`**
    *   **职责**：存放**全局通用**的 UI 组件（如：通用的弹窗、自定义按钮、空状态图、全局布局组件等）。
    *   **特点**：这里面的组件不包含具体的业务逻辑，可以在多个不同的页面中被重复调用。
*   **`src/router/`** (或 `routes/`)
    *   **职责**：配置页面的路由规则。它决定了用户在浏览器输入什么 URL（路径），页面就展示哪一个 `pages/` 里的组件。
*   **`src/api/`** (或 `services/`)
    *   **职责**：负责与后端进行数据交互，存放所有的 API 请求函数。通常会在这里封装 Axios 实例并管理所有的请求路径。
*   **`src/utils/`** (或 `helpers/`)
    *   **职责**：存放纯逻辑的通用工具函数（如：日期格式化、资金计算、手机号格式校验、本地缓存管理等）。
*   **`src/assets/`**
    *   **职责**：存放需要被打包工具编译的静态资源（如：项目内部使用的图片、SVG 图标、全局全局样式文件等）。

---

### 三、 要添加新功能时，代码如何分类？
作为新人，当收到“开发一个新页面”或“增加一个新功能”的任务时，可以按照以下标准步骤来组织自己的代码：

#### 步骤 1：创建页面组件（在 `pages/` 中）
在 `src/pages/` 下新建一个与功能相关的文件夹。例如要写一个“帮助中心”页面：
*   新建 `src/pages/HelpCenter/index.tsx`（编写页面主结构）。
*   如果这个页面很复杂，可以拆分出一些子组件。**注意**：这些只在“帮助中心”使用的子组件，请直接放在 `src/pages/HelpCenter/components/` 下，不要放进全局的 `src/components/`。

#### 步骤 2：配置路由（在 `router/` 中）
打开 `src/router/` 下的路由配置文件，引入你刚刚写好的 `HelpCenter` 组件，并为其分配一个路径（如 `/help`）。

#### 步骤 3：定义接口（在 `api/` 中）
如果新功能需要从后端获取数据：
*   在 `src/api/` 下新建（或修改）对应的接口文件（如 `help.ts`）。
*   编写并导出请求函数。
*   回到 `HelpCenter/index.tsx` 页面中，导入并调用该函数。

#### 步骤 4：提取通用部分
在写代码的过程中，如果你发现：
*   **UI 样式可复用**：某个卡片样式在其他页面也需要用到 $\rightarrow$ 将其抽取并移至全局的 `src/components/`。
*   **逻辑/计算可复用**：某段计算时间差的逻辑在其他地方也能用 $\rightarrow$ 提取成函数，放进 `src/utils/`。

---


## Next step for 2026 `Gemini生成，仅供参考`

### 1.User Authentication and Verification System

#### 技术栈推荐
* **前端**：Vue 3 / React + Axios（网络请求） + Pinia / Redux（状态管理，用于本地存储 Token）。
* **后端**：Spring Boot (Java) / NestJS (Node.js) / Gin (Go)任选。
* **数据库**：MySQL（主存储） + Redis（缓存、验证码、Token 黑名单管理）。
* **安全与协议**：HTTPS + JWT (Access Token & Refresh Token) / Session + Bcrypt (哈希加密)。


#### 实现原理（200字内简要说明）
> 本系统实现用户完整生命周期。**注册**时密码哈希加盐（Bcrypt）存入MySQL；**登录**成功则生成JWT返回前端用于身份鉴权；**登出**时清除前端Token，后端将当前Token记入Redis黑名单使其失效；**注销（销毁账号）**时，系统校验前置条件后对隐私数据逻辑删除、脱敏并去标识化（满足合规要求），同时作废该用户所有活跃Token，清理会话，待冷静期满后彻底完成数据闭环。


#### 四大核心功能详细设计

##### 1. 注册 (Registration)
* **流程说明**：
  1. 用户输入邮箱/手机号，通过滑块等防刷验证后，系统调用邮件/短信接口发送验证码（验证码在 Redis 中存储 5 分钟并设置 TTL）。
  2. 用户输入密码，前端校验密码强度。
  3. 后端校验验证码通过后，使用 Bcrypt 算法对密码进行单向哈希加密。
  4. 将加密后的密码与用户信息写入 MySQL，完成注册。

##### 2. 登录 (Login)
* **流程说明**：
  1. 用户输入账号、密码。
  2. 后端查询数据库，比对哈希值（`bcrypt.compare`）。
  3. 验证通过后，后端生成双 Token：**Access Token**（短期有效，如 2 小时，用于接口鉴权）和 **Refresh Token**（长期有效，如 7 天，用于刷新 Access Token）。
  4. 将 Token 返回前端，前端存入 `localStorage` 或 `Cookie` 中。

##### 3. 登出 (Logout)
* **流程说明**：
  1. 用户点击“退出登录”。
  2. **前端操作**：清除本地存储的 Access Token 和 Refresh Token，将用户重定向至登录页。
  3. **后端操作**：获取当前请求携带的 Access Token，将其存入 Redis **黑名单**中，设置生存时间为该 Token 的剩余有效时间。后续带有该 Token 的请求将被拦截器直接拒绝。

##### 4. 注销 (Account Deletion / Deregistration)
* **流程说明**：
  1. **前置校验**：系统检查该账号是否满足注销条件（如：无未完成订单、无未提现余额、无未处理纠纷等）。
  2. **确认申请**：用户通过验证码二次确认后，系统将账号状态修改为 `注销中`，并记录注销时间 `deleted_at`。
  3. **强制退登**：系统立刻吊销该账号名下的所有活跃 Token，将其列入黑名单，强制所有设备退出登录。
  4. **冷静期与匿名化（15天后）**：
     * **冷静期内**：用户如重新登录，可申请撤销注销。
     * **冷静期满**：定时任务（Cron Job）执行，将该用户的敏感隐私数据（如手机、邮箱、真实姓名）进行**去标识化（脱敏/匿名化）**处理（例如：将 `张三` 改为 `deleted_user_1028`），以便保留历史订单/账单数据进行对账，同时彻底销毁用户个人隐私信息。


#### 数据库核心字段设计 (MySQL)

| 字段名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `id` | bigint | 唯一主键 |
| `email` | varchar | 绑定邮箱（唯一索引，注销后可加随机后缀释放占用） |
| `password_hash` | varchar | 加密后的密码哈希值 |
| `status` | tinyint | 状态：1-正常，2-注销中，3-已注销 |
| `is_deleted` | tinyint | 是否删除：0-未删除，1-已删除（逻辑删除标记） |
| `deleted_at` | datetime | 注销/删除时间（用于冷静期计算） |

---

### 2.用账号发评论功能设计

#### 整体设计思路
核心目标是把**账号登录态（JWT）**与我们之前做好的**树状嵌套评论（自关联表）**连接起来。

* **安全原则**：前端调用发表/回复评论接口时，**不传递 `userId`**，后端统一从 Header 的 Token 中解密提取，防止抓包篡改。
* **数据原则**：保持评论树的拓扑结构完整。



#### 核心技术点与讨论议题（团队开发重点）

##### 1. 身份如何绑定（Auth 拦截）
* **流程**：
  * 前端请求拦截器自动在 `Authorization` 头部带上 Token。
  * 后端 Auth 中间件校验 Token，解析出当前用户的 `userId`，然后**挂载到 `req.user`（或全局上下文）**。
  * 写入数据库时，从 `req.user.id` 获取值写入 `comments.user_id`。

##### 2. 数据库怎么关联（加个外键）
评论表（`comments`）核心字段只需关联用户表和自身：
* `user_id`：关联用户表 ID（用于知道是谁发的）。
* `parent_id`：指向父评论 ID（用于构建您之前实现的树状结构，根节点设为 `null`）。

##### 3. 【重点讨论】账号注销了，评论树怎么处理？
当用户在系统里注销（销毁）账号时，他的树状评论**不能物理删除**。
* **为什么**：因为是树状结构（套娃回复），如果删了 A，后面回复 A 的 B、C、D 评论会因为找不到父级（`parent_id` 悬空）导致树结构断裂报错。
* **解决方案**：采用**逻辑匿名化**。注销时，后端把该用户的 `comments` 数据的 `user_id` 改为 `0`（系统占位用户），并将内容脱敏（如改为“该用户已注销”）。这样既抹去了隐私，又保全了树的完整性。

##### 4. 权限与越权控制
* **删除/修改自己发表的评论**：后端接口除了验证 Token 登录态，必须加上条件判断：`当前登录的 userId == 评论记录的 user_id`，防止他人通过拼接 API 恶意删除别人的评论。

